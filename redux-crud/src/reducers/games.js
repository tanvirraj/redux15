const initialState = [
    {'name': "Tanvir"},
    {'name': "Sumit"},
    {'name': "Masuq"}
]


export default function games(state=initialState, action={}){
    switch (action.type) {
        default:
            return state;
    }
}